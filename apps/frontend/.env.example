# Frontend environment variables
# Copy to .env.local if you want to override defaults (do NOT commit it)

# If you prefer explicit API base in production (otherwise use Vercel rewrites):
# VITE_API_BASE=https://your-backend.example.com
VITE_API_BASE=

# Optional: Sentry DSN for frontend error tracking
VITE_SENTRY_DSN=
